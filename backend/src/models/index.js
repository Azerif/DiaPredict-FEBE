// Models index file
// Export all models from this file

const UserModel = require('./user.model');
const HealthRecordModel = require('./healthRecord.model');
const PredictionModel = require('./prediction.model');
const TestimonialModel = require('./testimonial.model');

module.exports = {
  UserModel,
  HealthRecordModel,
  PredictionModel,
  TestimonialModel
};
